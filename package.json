{
  "name": "ci-pipeline",
  "version": "1.0.0",
  "description": "A simple CI pipeline project with GitHub Actions",
  "main": "src/index.js",
  "scripts": {
    "test": "jest"
  },
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
